<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UniHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorSurfaceContainer">@color/white_1000</item>
    </style>

    <style name="Theme.UniHub" parent="Base.Theme.UniHub" />
</resources>